<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.p1pmdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.DatosPersonalesActivity"
            android:parentActivityName=".UI.MainActivity"
            android:label="Datos personales" />
        <activity android:name=".UI.ModificarActivity"
            android:parentActivityName=".UI.MainActivity"/>
        <activity
            android:name=".UI.EstadisticasActivity"
            android:label="Estadisticas generales"
            android:parentActivityName=".UI.MainActivity" />
        <activity
            android:name=".UI.AddActivity"
            android:label="Añadir actividad"
            android:parentActivityName=".UI.MainActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>